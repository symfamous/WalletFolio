Mood characters for the Share card (components/ShareCardModal.tsx).

Drop two square PNG/SVG images here (transparent background looks best):

  happy-panda.png   -> shown when 24h change is >= 0  (green / "Up only" / "Euphoric")
  sad-cat.png       -> shown when 24h change is  < 0  (red / "Down bad" / "Shook")

Use your own watermark-free art. If a file is missing, the card falls back to
an emoji (🐼 / 😿) automatically — no error.

Recommended: ~512x512, transparent PNG.
